<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HotelTransylvaniaMaui.MainPage"
             BackgroundImageSource="hotel.jpg"
             Title="Hotel Transylvania Main Page">

    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Center">

            <Label
                x:Name="MyLabelInfoTwo"
                Text="Välkommen till hotell Transylvania!"
                FontFamily="Chiller"
                TextColor="DarkRed"
                SemanticProperties.HeadingLevel="Level1"
                FontSize="170"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                Margin="40"/>
            <Label
                Text="{Binding CurrentWeather}"
                FontFamily="Chiller"
                FontSize="90"
                TextColor="DarkRed"
                SemanticProperties.HeadingLevel="Level1"
                HorizontalOptions="Center"
                VerticalOptions="Center"/>

            <Button
                Text="Boka rum"
                TextColor="DarkRed"
                FontFamily="Chiller"
                FontSize="30"
                BackgroundColor="DarkSlateGrey"
                Clicked="OnClickedChangeToHotelLayout"
                Margin="20"
                Padding="10"
                WidthRequest="110"/>
            <Button
                Text="Admin"
                TextColor="DarkRed"
                FontFamily="Chiller"
                FontSize="30"
                BackgroundColor="DarkSlateGray"
                Clicked="OnClickedChangeToAdminPage"
                Margin="20"
                Padding="10"
                WidthRequest="110"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
